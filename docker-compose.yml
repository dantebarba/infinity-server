version: '3.7'

services:
  gdrive-mount:
    image: rclone/rclone:beta
    container_name: gdrive-mount
    restart: unless-stopped
    command: "mount gcache: /mnt/gdrive --config=/config/rclone/rclone.conf --allow-other --cache-db-purge --gid ${PGID} --uid ${PUID} --dir-cache-time 48h --buffer-size 32M --drive-chunk-size 16M --log-level ${LOG_LEVEL} --stats 1m --use-mmap --timeout 1h --vfs-cache-mode minimal --vfs-read-chunk-size 128M --vfs-read-chunk-size-limit 1G"
    volumes:
      - ${STORAGE_LOCATION}/gdrive:/config/rclone
      - /mnt/gdrive:/mnt/gdrive:shared
    privileged: true
    devices:
      - /dev/fuse
    cap_add:
      - MKNOD
      - SYS_ADMIN
    environment:
      PGID: ${PGID}
      PUID: ${PUID}
      LOG_LEVEL: ${LOG_LEVEL}
      TZ: ${DOCKER_TZ}

  transmission:
    image: "linuxserver/transmission"
    container_name: transmission
    hostname: transmission
    restart: unless-stopped
    volumes:
      - "${STORAGE_LOCATION}/transmission/config:/config"
      - "/mnt/gdrive:/media/downloads"
      - "/etc/localtime:/etc/localtime:ro"
      - "./wait-for-it.sh:/wait-for-it.sh"
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${DOCKER_TZ}
    ports:
      - "9091:9091"
      - "51413:51413"
      - "51413:51413/udp"
    depends_on:
      - gdrive-mount
